<%= form_for @salary_increment, :html => { :class => 'form-horizontal' } do |f| %>
  <% if @salary_increment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@salary_increment.errors.count, "error") %> prohibited this salary_increment from being saved:</h2>

      <ul>
      <% @salary_increment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= f.hidden_field :employee_id%>
  <div class="form-group">
    <%= f.label :date, :class => 'col-sm-2 control-label' %>
    <div class="col-sm-4">
      <%= f.text_field :date, :class=>'form-control date-field' %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :amount, :class => 'col-sm-2 control-label' %>
    <div class="col-sm-4">
      <%= f.number_field :amount, :class=>'form-control' %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :incremented_salary, :class => 'col-sm-2 control-label' %>
    <div class="col-sm-4">
      <%= f.number_field :incremented_salary, :class=>'form-control' %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :comments, :class => 'col-sm-2 control-label' %>
    <div class="col-sm-4">
      <%= f.text_field :comments, :class=>'form-control' %>
    </div>
  </div>
  <div class="actions">
    <%= f.submit 'Save' , class:"btn btn-primary " %>
  </div>
<% end %>
